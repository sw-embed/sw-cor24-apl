Implemented user-defined functions with del syntax (Phase 8.3). Monadic (del R <- FN X) and dyadic (del R <- X FN Y) definitions. Recursive calls supported up to 8 levels via call stack that saves/restores AST, token, and local variable state. eval_fncall() separated from eval() for stack efficiency. Parser detects function calls in value (monadic) and infix (dyadic) positions. Labels and goto work in function bodies. Added )FNS command, fn_reset in )CLEAR. New src/fn.h with function table, header parser, label scanner. Test 25-user-functions covers DOUBLE, ADD, FACT (recursive), nested calls. All 26 tests pass.