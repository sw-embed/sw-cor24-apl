Implemented tokenizer (src/tok.h) with 11 token types (NUM, IDENT, RES, PLUS, MINUS, STAR, SLASH, LPAREN, RPAREN, ASSIGN, EOL), 6 reserved words (rho, iota, take, drop, rev, cat), parallel-array storage, and tok_dump debug output. REPL updated to tokenize input and display token stream. Discovered and worked around tc24r preprocessor bug (inline comments on #define lines included in macro expansion). All test cases verified on COR24 emulator.