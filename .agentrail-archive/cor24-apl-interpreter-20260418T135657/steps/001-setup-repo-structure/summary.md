Created project directory structure (src/, asm/, test/) and build.sh that drives the full tc24r -> cor24-run toolchain pipeline. Wrote minimal main.c hello world that prints 'COR24 APL v0.1' over UART. Verified end-to-end: tc24r compiles C to COR24 assembly (1406 bytes), cor24-run assembles and executes it, outputting the banner and halting cleanly in 533 instructions. Added .gitignore for build/, updated README with build instructions.