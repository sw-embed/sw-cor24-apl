Create the project directory structure and build system. Create src/ for C source, asm/ for hand-written assembly, test/ for test scripts. Locate the tc24r compiler in ~/github/sw-vibe-coding/tc24r and the cor24 emulator in ~/github/sw-embed/sw-cor24-emulator. Create a Makefile or build.sh that compiles a trivial C program (hello world printing to UART) through tc24r to COR24 assembly, assembles it, and runs it on cor24-run. Verify the full toolchain pipeline works end-to-end. See docs/plan.md Phase 0, Step 0.1.