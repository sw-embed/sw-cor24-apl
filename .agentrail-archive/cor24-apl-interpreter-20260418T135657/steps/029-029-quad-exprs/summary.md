Verified quad variables work in arbitrary expression contexts. Created test 16-quad-exprs covering: cross-variable (qled <- qsw), toggle (qled <- 1 - qled), arithmetic (qsw + qled, qled + 100, qled * 3), variable capture (A <- qled + qsw), and self-assignment (qled <- qled). All 16 tests pass. No code changes needed — existing tokenizer/parser/evaluator already handles quad vars as first-class expressions.