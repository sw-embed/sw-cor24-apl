Added right-justified vector output with consistent column widths. Added num_width() to compute print width of integers and print_int_rj() for padded output. Vector display now scans all elements for max width, then right-justifies each to that width. Verified on COR24 emulator: mixed-width vectors like '100 _5 3000' display with proper alignment. Completes Phase 2 (vector support).