Implemented dyadic ceil (max) and floor (min) primitives as reserved words. Added RES_CEIL/RES_FLOOR to tokenizer, TOK_CEIL/TOK_FLOOR internal pseudo-tokens for reduce nodes, eval_binop_scalar max/min logic, NODE_DYAD dispatch with full scalar extension (scalar-scalar, vector-vector, scalar-vector, vector-scalar). Extended parser to detect ceil/ and floor/ as reduce operators. All 14 test cases pass: dyadic, element-wise, scalar extension, and max/min reduce.