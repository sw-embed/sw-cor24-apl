Implemented branch (goto) and labels for Phase 8.1. Added TOK_GOTO token, NODE_GOTO/NODE_CGOTO AST nodes, and branch_target evaluator logic. Labels (LABEL:) store line numbers as variables via arr_scalar(). Main loop rewritten with 64-line program buffer and PC-based execution for backward branching (loops). goto 0 exits interpreter. Two new tests: loop counting 1-10, sum accumulation with goto 0. All 24 tests pass.