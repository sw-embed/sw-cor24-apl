Implemented bump-allocated heap (4096 words) with array data structure in src/arr.h. 3-word header (rank, dim0, dim1) + data layout supports rank 0 (scalar), rank 1 (vector), rank 2 (matrix). API: arr_new, arr_get, arr_set, arr_scalar, arr_vector, arr_reset, arr_rank, arr_dim0, arr_dim1, arr_size. Verified on COR24 emulator: scalar(42), 5-element vector, 2x3 matrix all correct. Heap accounting validated (21 words used). Reset reclaims all. Integrated into main.c with arr_reset() at startup.